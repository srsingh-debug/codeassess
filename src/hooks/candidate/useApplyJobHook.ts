//get job details by jobID
// create hook form for apply job.
// handle apply job form submission.

import { useState, useEffect } from 'react';

const useApplyJobHook = ({jobId}: {jobId: string | undefined}) => {
      console.log(jobId);
  return {

  };
}

export default useApplyJobHook