openapi: 3.0.0
info:
  title: My Service API
  version: 1.0.0
  description: API exposed by my service
paths:
  /health:
    get:
      summary: Health check
      responses:
        '200':
          description: Service is healthy
